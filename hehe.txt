FOLDER A -> push repository
git add *
git commit -m "add text"
git push

FOLDER B -> clone repository
git clone http://ajaddksfsddsd

FOLDER B -> mengambil perubahan dari repository a
git pull






echo "# perpustakaan" >> README.md
  git init
  git add README.md
  git commit -m "first commit"
  git branch -M main
  git remote add origin https://github.com/Fuziahmad/perpustakaan.git
  git push -u origin main